# pnpm behaviour for this monorepo
strict-peer-dependencies=false
auto-install-peers=true
# Node in .nvmrc is 24; local machines may run 22.13+ and everything still works,
# so engine checks warn rather than fail. CI pins Node 24.
engine-strict=false
