/**
 * The modal slot renders nothing unless an intercepted route fills it — which is what a
 * direct visit, a refresh or any other page gets (Next.js parallel routes).
 */
export default function ModalDefault() {
  return null;
}
