# Decision Log (lightweight ADRs)

Format: `ADR-NNN — Title` · Date · Status (Proposed/Accepted/Superseded) · Context · Decision · Consequences.

## ADR-001 — Single Next.js app + separate worker
2026-09-10 · Accepted · See `01-project-analysis.md` D1, TRD §2.

## ADR-002 — Reminder engine evaluates at send slots, not per-member scheduled jobs
2026-09-10 · Accepted · Renewal/unsubscribe take effect automatically; eligibility re-checked before sending. Requires efficient candidate SQL and idempotency keys.

## ADR-003 — On-device face recognition in native Kotlin kiosk; licensed engine required for production
2026-09-10 · Accepted · Offline, fast, private. Research-only weights allowed only in POC. Final engine recorded in ADR-0xx after POC.

## ADR-004 — Post-expiry reminder cap (default 7 days), configurable
2026-09-10 · Proposed (needs owner sign-off, question D2) · Protects WhatsApp number quality; members beyond cap go to call list.

## ADR-005 — Gender field added to sign-up
2026-09-10 · Proposed (owner sign-off) · Pricing depends on gender.

## ADR-006 — Prisma 7 stable line; no RC versions
2026-09-10 · Accepted · Schema validated against 7.10 engine; datasource URL in `prisma.config.ts`; driver adapter `@prisma/adapter-pg`.

## ADR-007 — pg-boss instead of Redis/BullMQ
2026-09-10 · Accepted · One less stateful service.

## ADR-008 — Local encrypted volume storage by default
2026-09-10 · Accepted · Simple, in-India, backed up with restic; S3-compatible driver available.

## ADR-009 — Imported register members have WhatsApp opt-in false until confirmed
2026-09-10 · Accepted · Avoid messaging people who never consented; QR confirmation or desk consent enables reminders.

<!-- Add new decisions below. Record: face engine vendor & licence; WhatsApp provider (direct vs BSP) and number; final prices; template approval categories; domain. -->

## ADR-010 — Supabase Postgres (Mumbai), no local Docker
2026-09-10 · Accepted · **Context:** the development machine cannot run Docker, and the blueprint assumed a local `postgres:17` container plus Testcontainers for integration tests. **Decision:** use managed **PostgreSQL 17 on Supabase, `ap-south-1` (Mumbai)** as the database for development, accessed **only through Prisma** — no `supabase-js`, no anon key, no service-role key, no Supabase Auth/Storage/Realtime. `DATABASE_URL` is the Supavisor **transaction** pooler (:6543, pool max 5) for the `apps/web` runtime via `@prisma/adapter-pg`; `DIRECT_URL` is the Supavisor **session** pooler (:5432, pool max 3) for `prisma.config.ts` migrations, the seed script and the pg-boss worker. Optional `SHADOW_DATABASE_URL` (separate empty project) covers the case where the Supabase role lacks `CREATEDB`; optional `TEST_DATABASE_URL` (separate project) runs integration tests, which skip with a message when it is unset. CI runs integration tests against a Postgres 17 **service container**. `infra/docker/compose.dev.yml` is not created; Dockerfiles, Caddy and VPS scripts move to Phase 8.

**Verified against current official documentation (10 Sep 2026), not from memory:**
- Supabase's Prisma guide assigns the **transaction** pooler (:6543) to `DATABASE_URL` for serverless/auto-scaling runtimes and **session** mode (:5432) to migrations — matching the split above.
- Prisma's PgBouncer guide: the Schema Engine "is designed to use a single connection to the database, and does not support connection pooling with PgBouncer", so migrations must use a direct/session connection. It also **recommends against** setting `pgbouncer=true` for PgBouncer 1.21.0 or later, and driver adapters such as `@prisma/adapter-pg` bypass the flag.
- Supabase's Prisma troubleshooting page confirms Supavisor **transaction mode does not support prepared statements**. `@prisma/adapter-pg` uses node-postgres, which issues unnamed queries, so no flag is required. This is proven with a live query at Step 3; adding `pgbouncer=true` to `DATABASE_URL` is the documented fallback.
- Prisma 7 **removed the `directUrl` datasource property**; the migration URL is now `datasource.url` in `prisma.config.ts`, and **`shadowDatabaseUrl` moved into `prisma.config.ts` too** (it lived in `schema.prisma` up to Prisma 6). Our config therefore sets `datasource.url = env("DIRECT_URL")`.
- `prisma migrate dev` needs `CREATEDB` on the connecting role to build its shadow database; providers that forbid creating databases require a manually provisioned one via `shadowDatabaseUrl`. `prisma migrate deploy` needs no shadow database.

**Verified on the live Supabase project (2026-09-11):** Mumbai (`aws-0-ap-south-1`) session pooler on :5432 and transaction pooler on :6543; PostgreSQL 17.6; the connecting role is `postgres` — not superuser, with CREATEDB; Supabase's `anon` role exists. Through the transaction pooler, Prisma 7.10 with `@prisma/adapter-pg` ran 11 parameterised queries (5 sequential, 6 concurrent) with no prepared-statement errors and **no `pgbouncer=true`** — the assumption above is now confirmed, not inferred.

**Consequences:** unit tests must never touch a database; the seed must batch its writes because every round trip crosses the internet; Supabase's auto-generated Data API must be neutralised (ADR-011); production database hosting is re-decided in Phase 8 and nothing in the app depends on the answer.

## ADR-011 — RLS enabled on every public table, with no policies
2026-09-10 · Accepted · **Context:** Supabase auto-generates a PostgREST **Data API** over the `public` schema, callable with the project's anon key. Our tables would otherwise be readable by anyone holding that key. **Decision:** the first migration runs `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` on **every** table the application creates in `public`, with **no policies**, and every later migration does the same for new tables. Additionally, `public` is removed from the Data API's exposed schemas in the dashboard (manual). **Why it is safe:** PostgreSQL table owners bypass RLS unless `FORCE ROW LEVEL SECURITY` is set, and our Prisma role owns the tables — so the application is unaffected — while the `anon`/`authenticated` roles PostgREST connects as match zero rows. pg-boss is unaffected because it lives in its own `pgboss` schema, which the Data API does not expose. **Consequences:** a migration that forgets the `ALTER TABLE` silently exposes a table, so it belongs on the migration review checklist and in the pre-launch security checklist.

## ADR-012 — TypeScript 6.0.x, not the `latest` TypeScript 7
2026-09-10 · Accepted · **Context:** npm `latest` for `typescript` is **7.0.2**, but `typescript-eslint@8.70.0` declares `typescript: ">=4.8.4 <6.1.0"`. Adopting TS 7 would mean dropping type-aware linting, which is what enforces our `strict` / `noUncheckedIndexedAccess` / `exactOptionalPropertyTypes` conventions. **Decision:** pin **TypeScript 6.0.3**, the newest stable release inside the supported range. **Consequences:** revisit when typescript-eslint ships TS 7 support; fall back to 5.9.3 if Next 16 or the Prisma 7 client generator prove incompatible with 6.x.

## ADR-013 — Fee state is canonical in TypeScript; `v_member_fee` is a read-model
2026-09-10 · Accepted · **Context:** `database-design.md` §4.1 defines `v_member_fee` using `now()`, which conflicts with CLAUDE.md §2.2 (inject a clock, never call `new Date()` in domain code) and with `testing-strategy.md` §2 ("tests must not rely on `now()`"). **Decision:** `feeState(today, membership...)` in `packages/core` is the single canonical implementation. `v_member_fee` stays as a fast read-model for list queries and dashboard counts only, and an integration test diffs it against the core function over the same fixtures at fixed dates. **Consequences:** no business decision (a reminder, a call task, a kiosk greeting) is ever taken from the view alone; if they disagree, the view is the bug.

## ADR-014 — Fee state ignores a non-contiguous future membership
2026-09-10 · Accepted · **Context:** BR-4.2 derives fee state "from the membership covering today or the latest ended one", and adds "If an upcoming membership starts the day after, use its end date". `v_member_fee` instead takes the confirmed membership with the greatest `endDate`, which would report `PAID` for a member whose membership ended in March and who has a booking starting next January. **Decision:** core takes the membership covering today; failing that, the latest **ended** one; and it extends the effective end date through a future membership **only when that membership starts within one day of the current end** (a genuine renewal chain). This is the literal reading of BR-4.2, not a new rule. **Consequences:** the view carries the same guard; documented as a known divergence risk in the view/core diff test.

## ADR-015 — `ReminderRule.offsetDaysTo` is authoritative for the post-expiry cap
2026-09-10 · Accepted · **Context:** the cap exists in two places — `settings.postExpiryMaxDays` (BR-5.2, default 7) and `ReminderRule.offsetDaysTo` on the `POST` rule (schema, `null` = no limit). **Decision:** `offsetDaysTo` is what the engine evaluates; it is written from `postExpiryMaxDays` whenever settings are saved, and settings validation rejects a state where the two disagree. **Consequences:** one query path, no drift; the settings screen stays the owner-facing control.

## ADR-016 — BR-2.3 "effective per month" reading
2026-09-10 · Proposed (display only; owner may adjust) · **Context:** BR-2.3 says "`floor(pricePaise / durationMonths)` rounded to nearest ₹10", which specifies two conflicting roundings. **Decision:** the displayed figure is `round(pricePaise / months / 1000) × 1000` paise — e.g. ₹4,000 ÷ 3 = ₹1,333.33 → **₹1,330**. This value is for plan-card display only and never used to charge anyone; `Membership.pricePaise` is always the plan price copied at purchase (BR-2.8). **Consequences:** if the owner wants "₹1,330" to read "₹1,333", change one function and its tests.

## ADR-017 — Integrations write through ports, never through `packages/db`
2026-09-10 · Accepted · **Context:** the WhatsApp simulator is specified to write `MessageLog` rows, but `folder-structure.md` restricts `packages/integrations` to `shared` and `core/ports`, and that boundary is ESLint-enforced. **Decision:** add a `MessageLogWriter` port in `packages/core/src/ports`, implemented in `packages/db` and injected at the app boundary (`apps/*/container.ts`). The simulator depends on the interface only. **Consequences:** the dependency rule stays mechanically enforceable, and the simulator is unit-testable with an in-memory writer.

## ADR-018 — `/api/v1/health` reports the worker heartbeat
2026-09-10 · Accepted · **Context:** `api-specification.md` §3 specifies `{ ok, db, version }`, but the worker is a separate process with no inbound ports, so its liveness is only observable through the `WorkerHeartbeat` table the schema already provides. **Decision:** extend the response to `{ ok, db, worker: { ok, lastBeatAt, ageSeconds }, version }`, with `worker.ok` false when the last beat is older than **3 minutes** (the threshold the schema comment states) and top-level `ok` false if either component is down. No secrets, no counts. **Consequences:** Uptime Kuma gets one URL covering both processes; `api-specification.md` §3 is updated to match.

## ADR-019 — Toolchain pins: ESLint 10 with eslint-plugin-import-x; project-local pnpm store
2026-09-11 · Accepted · **Context:** (1) `eslint@9.39.5` installs with an upstream "no longer supported" deprecation, so the 9.x line is end-of-life. `eslint-plugin-import@2.32.0` caps its peer range at ESLint 9, and `eslint-config-next@16.3.4` depends on it. (2) The developer's global pnpm config sets `storeDir` to `D:\dev-cache\pnpm-store` while the repo lives on `E:`; hardlinks cannot cross volumes, so pnpm byte-copied every package and a first install ran for well over an hour. **Decision:** (1) pin **ESLint 10.10.0** and enforce the folder-structure import boundaries with **`eslint-plugin-import-x@4.17.1`** — a maintained, ESLint-10-native fork exposing the same `no-restricted-paths` rule named in `folder-structure.md`. `eslint-config-next` is dropped: the flat config never loaded it, and Next-specific lint rules can be added later via `@next/eslint-plugin-next` if wanted. TypeScript stays at 6.0.3 (ADR-012). (2) `storeDir: E:/.pnpm-store` in `pnpm-workspace.yaml` (pnpm 11 reads settings there, not from `.npmrc`), leaving the global config untouched. Packages allowed to run install scripts are listed explicitly under `onlyBuiltDependencies` (Prisma, esbuild, Tailwind oxide, unrs-resolver). **Consequences:** a developer on a different drive layout should adjust or remove `storeDir`. On Linux (CI) the value is not a drive path; pnpm is expected to treat it as a repo-relative directory named `E:`, which is git-ignored — **unverified until the first CI run**, and the fallback is to override the store in `ci.yml`.

## ADR-020 — Extensionless relative imports in TypeScript source
2026-09-11 · Accepted · **Context:** workspace packages are consumed from TypeScript source, not from a built `dist` (`transpilePackages` in `apps/web/next.config.ts`). The first `next build` failed with 52 "Module not found" errors: Next 16's Turbopack does not map a `./foo.js` specifier onto `./foo.ts` inside those packages, although `tsc`, Vitest and tsx all do. **Decision:** relative imports in `apps/*` and `packages/*` omit the file extension (`./constants`, not `./constants.js`). This is the form `moduleResolution: "Bundler"` (set in `tsconfig.base.json`) is designed for, and every tool in the stack resolves it. **Consequences:** the packages cannot be run by plain Node ESM without a loader or bundler — which is already the case, since nothing in the repo executes untranspiled TypeScript; the worker runs under tsx, and the Phase 8 container build bundles it. Revisit only if a package is ever published or executed as raw ESM.

## ADR-021 — `sm` is 360px: the first multi-column layout starts at `md`
2026-09-11 · Accepted · **Context:** `design-tokens.json` defines `breakpoint.sm = 360px`, and `tokens.css` maps the breakpoint tokens onto Tailwind v4's `--breakpoint-*` variables, as it does for every other token group. That overrides Tailwind's default `sm` of 640px, so any `sm:` utility applies on every supported phone. The Phase 1 Foundation page used `sm:grid-cols-2` and rendered two columns at 360px, clipping content. **Decision:** keep the token as designed (design docs are the source of truth, CLAUDE.md §1). `sm:` therefore means "all supported phones" and adds nothing over the unprefixed class; multi-column layouts begin at `md:` (768px), and `lg:` (1024px) / `xl:` (1280px) match Tailwind's defaults. **Consequences:** UI code and copied shadcn-style components must not rely on `sm:` as a tablet breakpoint; review any imported component's `sm:` classes when it is restyled to tokens. Every screen is still checked at 360px (CLAUDE.md §2.10).

## ADR-022 — Landing data: tagged `unstable_cache`, HTML rendered per request
2026-09-11 · Accepted · **Context:** TRD §4 asks for the landing page to be static with ~5-minute revalidation, refreshed by tag when the owner changes prices, promo or settings. In Next 16 the tagged `'use cache'` directive requires `cacheComponents: true`, which changes rendering rules app-wide (dynamic APIs need Suspense boundaries), and `revalidateTag` now takes a cache profile as a second argument. The page also shows date-dependent content — today's hours row and "Open today till…" — and a fully static build would need database access at `next build` time, which CI does not have. **Decision:** data (plans, gym settings) is read through `unstable_cache` with tags `plans` and `settings` and a 300-second revalidate; the HTML renders per request from that cached data. `revalidateLandingContent()` calls `revalidateTag(tag, 'max')` for the CRM to use when settings change (Phase 4) — there is no public revalidation route. If the database is unreachable, sections that need it render their designed fallback rather than failing the page. **Consequences:** freshness matches the TRD (≤ 5 min, instant on tag); render cost is a cached read, not a query; revisit `cacheComponents` when the rest of the app is ready for it.

## ADR-023 — New-lead owner alert is recorded now, delivered later
2026-09-11 · Accepted · **Context:** PRD LP-04 expects the owner to get a WhatsApp alert within a minute of a lead. The outbox dispatcher arrives in Phase 3 and the WhatsApp engine in Phase 6. **Decision:** `submitLead` writes the Lead, a `NEW_LEAD` bell Alert and an `alert.owner` OutboxEvent (dedupe key `lead-alert:{leadId}`) in one transaction. Until the dispatcher exists, the alert is visible in the CRM bell and waits in the outbox; once it ships, queued events are delivered without data migration. Bot protection in the meantime is a honeypot plus a minimum fill time; flagged submissions get an identical success response and nothing is stored (Turnstile decision stays in Phase 8, security-plan §3.1). Rate limits follow api-specification §8 (10/h per IP, 3/h per mobile), not the 5/h in the endpoint section.

## ADR-024 — Announcement bar and promo banner are separate settings
2026-09-11 · Accepted · **Context:** PRD LP-02 (announcement bar) and LP-11 (mid-page promo banner) are both "driven by promo settings", and the copy deck gives them different text ("First session free…" vs "Festive offer…"), but `GymSettings.promo` held a single text. **Decision:** keep the existing `enabled/textEn/textHi` for the banner and add `barEnabled/barTextEn/barTextHi` (≤ 90 characters) for the bar, both sharing the optional `startDate/endDate` window. The new fields default to off, so settings saved before the change still parse unchanged. `activePromos()` in packages/core decides what shows: date window inclusive in IST, fall back to the other language when one text is blank, never render an empty bar or banner, ignore a malformed date rather than hide the offer. **Consequences:** the CRM promo screen (Phase 4) edits two short texts instead of one.

## ADR-025 — `cn()` knows the design-token scales
2026-09-11 · Accepted · **Context:** `cn()` runs tailwind-merge, which recognises Tailwind's default scales only. It read token utilities such as `text-display-l` or `text-body` as text colours, decided they conflicted with `text-brand-plate-navy` or `text-brand-white`, and silently dropped one. Section headings rendered at body size, and red buttons lost their white text (axe: 4.49:1 and 3.56:1 on Signboard Red). **Decision:** `apps/web/src/lib/cn.ts` uses `extendTailwindMerge` with the token names for `text` (font sizes), `leading`, `radius`, `font` and `ease`. A unit test pins the combinations that broke. **Consequences:** a new font-size, line-height or radius token must also be added there; the test fails loudly if a size and a colour stop coexisting.

## ADR-026 — Landing page composition
2026-09-11 · Accepted · **Context:** Phase 2 wireframes list a timings/visit section (§12) and a separate contact block (§15) that repeat the same address, call, WhatsApp and directions actions. The Phase 1 foundation page occupied `/`. **Decision:** (1) Contact is folded into the visit section, which carries the `#contact` anchor and the click-to-load map. The gym's address line already names the landmark, so no separate landmark line is shown. (2) The foundation check moves to `/foundation` and returns 404 in production. (3) `/join` is a placeholder until Phase 3; every Sign up and Choose button already links there with `?plan=CODE`. (4) All derived values (contact, hours table and summary, "open today", promos, monthly prices, which sections exist) are computed once per request in `lib/site-context.ts`. (5) Client components receive only the message namespaces they use (`CLIENT_NAMESPACES` in the locale layout). (6) The accessory removed in the design critique is the gold rule under the champion title: the title, quote bar and timeline years already carry the gold. **Consequences:** one fewer section to maintain; the navigation's section list omits Champion and Reviews when they are not rendered.

## ADR-027 — Consent banner appears only when an analytics provider is configured
2026-09-11 · Accepted · **Context:** PRD LP-25 requires analytics to fire only after consent. No Plausible domain or GA4 ID is configured yet. **Decision:** analytics scripts load only after "Allow analytics". The banner renders only when `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` or `NEXT_PUBLIC_GA4_ID` is set, because asking consent for analytics that do not exist is noise. Decline and accept are equal-weight buttons. Links carry `data-track` attributes, and one delegated listener reports PRD §7 events through `track()`, which does nothing without consent. Event properties carry context only (source, plan, goal), never personal data. **Consequences:** setting either variable later turns the banner on with no code change; the privacy policy already describes consent-based analytics.

## ADR-028 — Legal pages and indexing
2026-09-11 · Accepted · **Context:** legal drafts need owner and lawyer review, and a machine or unreviewed Hindi legal translation would be risky. Development and demo deployments carry placeholder content. **Decision:** Privacy, Terms and Refund are English Markdown files in `apps/web/content/legal/`, rendered by a small parser to React elements with no raw-HTML path; links are limited to https, mailto, tel and site paths. The Hindi routes show a notice and the English text. A "DRAFT — pending client review" banner appears wherever unconfirmed content is allowed. Search indexing is allowed only when `NODE_ENV=production` and `DEMO_MODE=false`; everything else is `noindex` and `robots.txt` disallows all. ExerciseGym JSON-LD omits `aggregateRating` (content strategy §4) and `geo` until the map pin is confirmed. The share image is English-only because the image renderer's font has no Devanagari. **Consequences:** Hindi legal text is a client-review item; turning indexing on is part of go-live, not a separate change.

## ADR-029 — End-to-end test scope for Phase 2
2026-09-11 · Accepted · **Context:** journey 2 ("lead form submit → CRM shows lead + call task") needs the CRM, which arrives in Phase 4. A real submission writes to the development database and counts against the per-IP limit (10 per hour). `next dev` compiles routes on first request, and parallel workers timed out against it. **Decision:** journey 2 asserts the success state now; the CRM half is added in Phase 4, and lead, alert and outbox writes are covered by core unit tests. The real submission runs in the desktop project only. Local runs use one worker and longer timeouts. In CI, E2E, axe and Lighthouse run on pushes to `main` against `next start` with a seeded Postgres 17 service container (testing-strategy §8). **Consequences:** that CI job is unverified until its first run on GitHub.

## ADR-030 — Hero LCP: poster first, video after load; narrow client imports
2026-09-11 · Accepted · **Context:** the first Lighthouse CI run on the production build measured mobile LCP at about 5.0 s against the 2.5 s budget (CLS 0, accessibility passing). The LCP element was the hero `<video>`, mounted at hydration and painted seconds later under CPU throttling. The poster `<img>` painted early but did not count: the flat placeholder AVIF was 953 bytes over 412×640 px, under Chrome's low-entropy threshold for LCP candidates. Separately, the lead form imported the `@mfp/shared` root, shipping the env schema and date helpers to the browser, about 90 KB of unused JavaScript. **Decision:** (1) the hero mounts video only after the window `load` event and an idle callback, so the poster is always the first large paint and later video can no longer become the LCP. (2) Placeholder hero media carry a faint static grain, and AVIF posters are encoded at CRF 18. Their entropy is then comparable to a real photo, so the budget measures what real posters will do. The grain is placeholder-only; real footage replaces the files by name. (3) `@mfp/shared` declares `sideEffects: false` and exports `./schemas/lead`; client components import narrow entry points, never the package root. (4) The second Lighthouse run showed the poster *loaded* by about 1.5 s but *painted* about 3 s later, behind hydration, plus a large parse/style/layout task. So the first slide's posters are preloaded from `<head>` per orientation with high fetch priority and decoded synchronously. The FAQ uses native `<details>`/`<summary>` instead of the Radix accordion from the Phase 2 plan: keyboard and screen-reader behaviour comes from the browser, and one hydrated island and its library leave the page. `@radix-ui/react-accordion` stays installed pending a CRM need. **Consequences:** video starts a moment after load, with the poster showing until then; any new client-side import from `@mfp/shared` should use a narrow export. This machine's CPU benchmark index is about 1,000–1,300, below the high-end desktop Lighthouse's 4× slowdown assumes, so local results are somewhat pessimistic. The budget is not relaxed for that; CI numbers are the reference.

## ADR-031 — Site facts come from the Google Business Profile; no "national champion" claim
2026-09-11 · Accepted · **Context:** the client supplied the gym's Google Business Profile and said to take everything from it and decide the rest ourselves (owner: Ajay, +91 98714 06350). The blueprint's positioning ("champion-led", a "National Champion" plaque, hero slide, trust item) assumed a title nobody had evidenced. Research on 2026-09-11 covered the Google listing and all visible reviews, Maps, Justdial snippets, Magicpin, and web searches for the owner. Findings: listing "Max Fitness GYm India", 4.8★ from 231 reviews; Mon–Sat 4:30 am–10 pm, Sunday closed; pin 28.633763, 77.350075, place ID `ChIJnaq6hDTlDDkRYcBqPGc91JU`. The owner is Ajay Kuliyal, named in reviews, who coaches with a team of trainers. The owner's own note says personal training costs ₹3,000 a month including diet. Reviews describe a beginner-friendly, value-for-money, female-friendly floor with a separate cardio area. A 2025 two-star review reports no female washroom, no changing rooms and a crowded floor. **No source mentions a national championship.** Google shows PIN 201020, while Justdial, Magicpin and postal references for Nyay Khand 1 give 201014. **Decision:** (1) Reposition on verified facts: "owner-coached since 2000", beginner-friendly trainers, the Google rating from settings. The champion plaque becomes a factual owner section, with a member's review quoted word for word. (2) Seed and fallback use the Google hours and address line with PIN 201014; map, directions and structured data use the verified pin and place ID; JSON-LD gains `geo`, `hasMap` and `sameAs`. (3) Testimonials are six real 5★ Google reviews: first name and initial, exact text, ratings checked on the listing. (4) Personal training and diet plans are confirmed extras. "Changing area" is removed. The timings and personal-training FAQs are verified. The unverified free first session and 2-hour call-back promises are removed from live copy. (5) Google photos are not reused: most are customer uploads, and the owner's own uploads show members' faces without marketing consent. **Consequences:** copy deck and content strategy updated to match. Owner to confirm: the PIN (and correct the Google listing if 201014), female washroom and changing-room facts before any women's FAQ answer, a free trial if offered, original photos, and any championship with proof (which would restore the claim). Quoting public reviews on the gym's own site follows the copy deck's rule of real reviews with the owner's go-ahead, here given through the client.

## ADR-032 — Public pages are statically cached; only today's hours row is decided in the browser
2026-09-11 · Accepted (supersedes the "HTML rendered per request" part of ADR-022) · **Context:** after ADR-030, mobile LCP was still about 5.0 s. Per-request rendering cost 0.9–1.3 s of TTFB under throttling, and the poster preload reached the browser only in the streamed body. The client chose to fix LCP before Phase 3, with a static landing page as the first option. Only three things on the page depended on the time of day: "Open today till…" in the trust strip, "Open till…" in About, and today's row in the hours table. **Decision:** (1) The locale layout's `generateStaticParams` returns `[]`, and the landing, contact and legal pages export `revalidate = 300` without `connection()`. Per the Next 16 docs, each page is rendered statically on its first visit, served from the cache, and regenerated at most every 5 minutes or at once via the `plans`/`settings` tags. `next build` prerenders nothing, so it never needs the database. `/join` stays dynamic because it reads `?plan=`. (2) The trust strip and About show the weekly hours line ("Mon–Sat 4:30 am – 10:00 pm") instead of today's status. (3) `HoursTable` is a small client component: it renders without a highlight and marks today's row using `dayOfWeek(toISTDate(new Date()))` from `@mfp/shared/time` after hydration, so the row is right in India whatever the visitor's time zone. The change is a background and a badge, with no layout shift. **Consequences:** changes to prices, promo or hours appear within 5 minutes, or instantly once the CRM revalidates tags. The promo window and footer year can lag a day boundary by the same margin. A cached page built while the database was unreachable serves the designed fallback for up to one revalidation window. Any page under `[locale]` that reads request-time data (`searchParams`, cookies, headers, `connection()`) must export `dynamic = 'force-dynamic'`. Otherwise the static segment throws at runtime in production and returns 500: `/join` did until this was added; it is caught by the axe and route checks.

## ADR-033 — First-paint budget: Latin-only font preloads, deferred section rendering, JavaScript on demand
2026-09-11 · Accepted · **Context:** with static caching (ADR-032), TTFB and poster loading were fast, yet first paint arrived at ~4.1 s under real (DevTools) 4G + 4× CPU throttling. The layout preloaded 10 font files (~240 KB, mostly Devanagari) at high priority, which saturated the link. Parsing, styling and laying out the long page, plus ~306 KB of JavaScript, then held the paint back. **Decision:** (1) Each font is split into a Latin face (preloaded) and a Devanagari face (not preloaded), composed in `--font-display`/`--font-body`. Weights are trimmed to Khand 700 and Hind 400/600, and the browser resolves 600/500 requests to the nearest loaded weight. Preloads drop from 10 to 3. (2) Regular sections, the owner section and the footer use `content-visibility: auto` with `contain-intrinsic-size: auto 900px` (`.deferred-render`), so off-screen content costs nothing until approached. (3) The lead form loads its Zod schema on first focus or validation, rendering from a Zod-free `@mfp/shared/schemas/lead-constants`. The mobile menu and gallery lightbox load Radix Dialog only when opened, returning focus to their trigger on close. **Measured (production build, this machine):** real throttling FCP = LCP 4.1–4.4 s → 3.0–3.4 s (fonts) → **2.2–2.3 s** (deferred rendering + lazy JS). Script transfer 306 → 207 KB; CLS 0. The Lighthouse CI simulated estimate went 4.8 → 3.9 → 3.5 s on `/` and 4.0 s on `/hi`, still above 2.5 s, because it attributes 82–88% of LCP to render delay behind script execution. **Gate (client decision, 2026-09-11):** Lighthouse CI enforces LCP ≤ 2.5 s and CLS ≤ 0.1 with real DevTools throttling (`lighthouserc.json`). The simulated estimate runs as a report-only warning (`lighthouserc.simulated.json`, `continue-on-error` in CI). Real-user data (CrUX / analytics web vitals) confirms after launch. **Fonts, revised the same day:** `next/font` was replaced by self-hosted files in `public/fonts` (the same Google Fonts files, SIL OFL, licence texts alongside). Two `next/font` instances per family had emitted duplicate `@font-face` sets, so every preloaded file was downloaded a second time. Now `styles/fonts.css` declares one face per weight and subset, and the layout preloads per language: Latin faces on English pages; the Devanagari Hind 400 and Khand 700 faces, plus Latin, on Hindi pages. Files carry `.v1` in the name and are served `immutable` for a year. Measured with real throttling: duplicate font downloads 0; `/` LCP median 2.38 s (passes); `/hi` 2.93–3.03 s, down from 3.5–5.1 s. `/hi` is still limited by a ~1.1 s layout task (was ~1.7 s). One more attempt, skipping layout of the off-screen hero slides, did not help: `/hi` measured 3.05–3.14 s. It was reverted. **Budgets (client decision, 2026-09-11):** English pages are gated at LCP ≤ 2.5 s, Hindi pages at LCP ≤ 3.0 s for now (`assertMatrix` in `lighthouserc.json`); CLS ≤ 0.1 and accessibility ≥ 0.95 for both. Two local runs put the `/hi` median at 2.94 s and 3.12 s, so on this machine the Hindi gate sits at its limit and may fail intermittently. CI hardware and real-user data will show whether the Hindi layout cost needs more work (candidates: less Devanagari text above the fold, a smaller hero form). **Consequences:** Hindi pages load Devanagari fonts on first use, with a brief swap. Adding a font weight or a preloaded subset now has a measurable first-paint cost. Client components should keep heavy libraries behind dynamic imports.

## ADR-034 — Payment confirmation: one core path, two row locks, webhook as source of truth
2026-09-11 · Accepted · **Context:** Phase 3 has three ways a payment can be confirmed: the browser's checkout callback, the Razorpay webhook and the DEMO_MODE pay dialog. They can arrive in any order, or at the same moment (cases P1–P3), and the receipt series must have no gaps (BR-11.2). **Decision:** (1) All three end in `confirmPayment()` in `packages/core`, in one transaction. `PrismaPaymentConfirmationUnitOfWork` takes `SELECT … FOR UPDATE` on the payment row first, so a second arrival waits, then reads `PAID` and returns `ALREADY_CONFIRMED`. The receipt and member-code counters increment with `INSERT … ON CONFLICT DO UPDATE … RETURNING`, which locks the counter row until commit, so a rolled-back confirmation gives its number back. (2) `verifyCheckout` checks the HMAC first, then reads amount and status from the gateway's own record (`GET /v1/payments/{id}`), never from the request. It confirms only `captured`; `authorized` returns `PENDING`, and the page polls `/checkout/status` until the webhook lands. (3) The webhook verifies the HMAC over the raw body before parsing, and records each event in `WebhookEvent` (unique `provider+externalId`; the `X-Razorpay-Event-Id` header, else `event:paymentId`). An event recorded but never processed (the process died, or confirmation threw) is **not** treated as a duplicate, so Razorpay's retry is processed. A payment for an order this system never created is marked processed with `PAYMENT_NOT_FOUND` and answered 200. Signature failures return 401. (4) An amount mismatch (P4) leaves the payment `CREATED` with `failureReason = AMOUNT_MISMATCH`, activates nothing, and queues `alert.owner {kind: PAYMENT_AMOUNT_MISMATCH}`; the member sees `NEEDS_REVIEW`, not "failed". (5) `payment.failed` sets a `CREATED` payment to `FAILED` only, never a paid one (Razorpay does not guarantee event order). A later capture on the same order still confirms, because Razorpay allows retries within an order. (6) In DEMO_MODE the webhook route returns 404: the simulator's "signature" is a fixed marker, so accepting webhooks would let anyone confirm a payment. The pay dialog (`POST /checkout/simulate`, which needs the member's token) completes the simulated order and returns Razorpay-shaped callback fields, and the browser posts them to the real `/checkout/verify`. (7) The existing outbox event types are reused: owner alerts are one `alert.owner` type with a `kind` in the payload, and enrolment is `kiosk.enroll`, whose dispatcher checks face consent and minor status. **Verified:** integration tests P1–P8 against PostgreSQL. With `FOR UPDATE` removed, P3 fails with two `CONFIRMED` outcomes, a double activation. **Consequences:** anything that confirms money later, such as the CRM desk payment, must go through the same locks and counters.

## ADR-035 — Sign-up checkout details not fixed by the spec
2026-09-11 · Accepted · **Context:** the spec leaves several Phase 3 details open. **Decision:** (1) **Email is optional** at registration: the privacy notice lists it as optional, and the gym does not need it. (2) **Pay-at-reception hold** is derived as `membership.createdAt + 48 h` (`PAY_AT_RECEPTION_HOLD_HOURS`), not stored. (3) **Order retries** reuse an unpaid membership only when plan, dates, price and admission fee all match and it was created within the 48 h hold. Otherwise a price change between attempts would leave the membership recording one fee while the payment charges another (BR-2.8), or revive an expired hold. Each retry creates a new `Payment` and gateway order, because Razorpay orders are not idempotent. (4) **Gender and plan:** an order for a plan from the other gender's price list is refused with a new domain code, `PLAN_GENDER_MISMATCH` (BR-2.5). (5) **Renewals** are allowed for `ACTIVE` and `LEFT` members (BR-4.4). A `LEFT` member who pays is returned to `ACTIVE` with `leftAt`/`leftReason` cleared, so the retention sweep does not delete a returning member's photo. The WhatsApp unsubscribe flag is left alone. A membership the nightly job cancelled is still confirmed if money arrives for it. (6) **Tokens:** the registration token is sent as `X-Registration-Token`, the renew token as `X-Renew-Token` (headers, never URLs). Receipt links (`/r/{token}`, purpose `receipt`, subject = payment id) last **90 days**, because members reopen them from WhatsApp weeks later. (7) **Selfie uploads:** the declared type is ignored. JPEG/PNG/WebP magic bytes are required, the image must decode (≤ 40 MP) with a short side ≥ 240 px, EXIF orientation is applied, and it is re-encoded with sharp to a ≤ 720 px JPEG with no metadata. Storage keys are random (`selfies/…`). If the database write fails, the stored object is deleted. (8) **Consent IP** is stored as an HMAC keyed with `LINK_TOKEN_SECRET`, not a plain hash, which could be reversed over the IPv4 space. (9) **Rate limits:** `/registrations` 10/h per IP and 3/h per mobile (spec). `/checkout/orders` and `/checkout/verify` 20 per 10 min per IP and `/checkout/status` 120 per 10 min per IP; the spec has no row for these. (10) **Razorpay adapter** uses `fetch` against the REST API with no SDK and no retries (order creation is not idempotent), a 10 s timeout, and Zod-validated responses. Payment ids must match `pay_[A-Za-z0-9]+` before they reach a URL. Gateway failures surface as 502 `PAYMENT_PROVIDER_UNAVAILABLE`, without the provider's wording. (11) **The container lives on `globalThis`** like the Prisma client, because the simulated gateway keeps orders in memory and Next may evaluate the module per route bundle and on every dev reload. A server restart between order and pay dialog returns 409 "start again". (12) **P9 (desk payment void) moves to Phase 4**, with the CRM fee desk that owns the staff actor, PIN elevation and audit row it needs. **Consequences:** the web tier's in-memory rate limits and simulator assume a single instance (as ADR-010 already does for rate limits).

## ADR-036 — Sign-up, renewal and receipt pages; worker jobs for Phase 3
2026-09-12 · Accepted · **Context:** the Phase 3 prompt asks for `/join` steps, a selfie sheet, renew and receipt pages, the outbox dispatcher, receipt PDFs and a `SIGNUP_NOT_PAID` partial. Several details were open or turned out differently in testing. **Decision:** (1) **`/join`, `/join/plan`, `/join/pay`, `/join/done` are full pages; the intercepted modal `@modal/(.)join` is deferred.** A parallel-route slot in the locale layout would put the modal's code and a second render path into the statically cached landing page (ADR-032/033). On a phone, where most sign-ups happen, the modal would be full-screen anyway. Every "Sign up" and "Choose" link already goes to `/join`. Revisit once the landing page's first-paint budget has headroom. (2) **Session state** `mfp_join` in `sessionStorage` holds only the registration token, first name, gender, minor flag, WhatsApp choice, plan, start date, and then a payment id or a reservation. Every read is validated field by field. `/join/done` reads the payment's status again from the server, so it survives a refresh and never shows a receipt that was not issued. (3) **Selfie sheet:** the explainer offers "Use phone camera" next to "Open camera", so someone who would rather not grant the camera is not stuck (E2E journeys 4 and 5 found the gap). MediaPipe's WASM runtime is copied from the pinned package into `public/mediapipe/wasm` on dev/build (gitignored, ~11 MB). The BlazeFace short-range model is committed with its licence note. Both load only when the camera opens; if the detector has not loaded within 4 s, capture is allowed without the check. (4) **E2E camera fixture:** `face.y4m` (one frame, looped by Chrome) and `face.jpg` are generated from a drawn SVG face by `scripts/make-face-fixture.mjs` in Playwright's global setup and are not committed. The real detector finds the drawn face in Chromium. No photograph of a person is used. (5) **Renewal:** `/renew/{token}` shows the BR-3.4 start date as fixed (no picker), with no admission fee. The page and `GET /api/v1/renew/{token}` share `loadRenewalOffer`. Journey 6 mints its own renew token, because renew links are issued by the reminder engine (Phase 6). (6) **Receipts:** `/r/{token}` renders from `PrismaReceiptReader` (never indexed, not cached, `no-referrer`). The worker renders an **English A5 PDF in Helvetica**, which has no ₹ or Devanagari glyphs, so amounts read "Rs. 4,000.00". Amount in words uses the Indian system (`amountInWordsINR`: lakh, crore). The PDF is served only through `GET /api/v1/receipts/{token}/pdf` with the same receipt token. (7) **Proxy matcher:** static files are now excluded by known extension instead of "any dot", because signed links contain a dot between the token body and signature and must still be localised (`/r/…`, `/renew/…`). (8) **Local storage root:** a relative `STORAGE_LOCAL_PATH` resolves from the workspace root (`resolveStorageRoot`). Before, the web app (`apps/web`) and the worker (`apps/worker`) wrote to different folders, and the web app could not serve PDFs the worker stored. Development selfies were moved to `./storage`. (9) **Payments on the CRM bell:** confirmation writes an `ONLINE_PAYMENT` alert in its transaction, and an amount mismatch writes one `SYSTEM` alert. A repeated mismatch delivery is recognised by `failureReason` and raises nothing new. (10) **Outbox dispatcher:** it claims only event types that have a handler, with `FOR UPDATE SKIP LOCKED` plus a 5-minute lease. In Phase 3 that is `receipt.pdf` → pg-boss `receipt-pdf` (singleton key = dedupe key). `whatsapp.receipt`, `kiosk.enroll` and `alert.owner` stay PENDING until Phases 6/7, so they are dispatched then instead of being lost. Retries back off at 30 s, 2 min, 10 min, 1 h and 6 h, then FAILED, recording a code, never a message. (11) **`SIGNUP_NOT_PAID`:** raised once per sign-up (never again after staff close it), for online sign-ups registered between 7 days and 24 hours ago, scoped to the gym by `GYM_SLUG`. The rest of the nightly job and the 7-day reservation cancellation are Phase 4. (12) **Analytics:** the PRD §7 sign-up and renewal events were added to the allowed list; none carries personal data. (13) Every step and token page renders per request (`force-dynamic`); robots disallow `/join`, `/r/` and `/renew/`. **Consequences:** WhatsApp receipt sends queue up in the outbox until Phase 6 lands. The PDF is English-only until a Devanagari font is embedded. The development database now holds E2E members ("Esha Tester", "Ravi Renewer") with real receipt numbers from local runs.

## ADR-037 — Payments stay in DEMO_MODE until delivery
2026-09-12 · Accepted (client decision) · **Context:** Razorpay KYC and live keys are not ready, and the client wants the demo gateway shown for now, with real Razorpay connected at delivery. **Decision:** `DEMO_MODE=true` remains the working and demo setting: "Pay" opens the in-app dialog, which completes a simulated order and posts Razorpay-shaped fields to the real `/checkout/verify`, so the verification, confirmation, receipt and outbox path in use is the same one real payments will take. The webhook route stays 404 in demo mode (ADR-034). **Switching at delivery** is configuration, not code: set `RAZORPAY_KEY_ID`, `RAZORPAY_KEY_SECRET` and `RAZORPAY_WEBHOOK_SECRET`, set `DEMO_MODE=false`, and point the Razorpay dashboard webhook at `/api/v1/webhooks/razorpay` for `payment.captured`, `order.paid` and `payment.failed`. `parseEnv` already refuses a production build with `DEMO_MODE=false` and no key secret. **Consequences:** the Razorpay adapter has been exercised only by its unit tests until those keys exist; a test-mode run through the real gateway is the first delivery task. Payments made in demo mode carry `method = SIMULATED` and read "Demo payment (no money taken)" on the receipt, so they are obvious in the CRM and must not be mistaken for income.

## ADR-038 — The sign-up modal ships; private media route (supersedes ADR-036 point 1)
2026-09-12 · Accepted · **Context:** ADR-036 deferred the intercepted modal (`@modal/(.)join`) because a parallel slot might cost the statically cached landing page its first-paint budget (ADR-032/033). It was built and measured instead of assumed. **Decision:** (1) **The modal ships.** `[locale]/@modal` holds `default.tsx` (null) and intercepted routes for `/join`, `/join/plan`, `/join/pay` and `/join/done`. Tapping "Sign up" or "Choose" on the landing page opens the sheet over the page; the same URL opened, refreshed or shared directly renders the full page; closing goes back. **Measured after the change (production build, DevTools throttling, medians of 3):** `/` LCP **2.34 s** (budget 2.5 s) and `/hi` **2.91 s** (budget 3.0 s), both slightly better than the ADR-033 baseline — the extra slot renders `null` and adds no client JavaScript to the landing page. (2) **The confirmation is intercepted too.** A parallel slot keeps its content across a client-side navigation, so without `(.)join/done` the pay step stayed on screen behind the confirmation. In the modal flow the confirmation therefore appears in the sheet; opened directly it is the full page. (3) **`GET /api/v1/files?key=&expires=&sig=`** serves private media behind the storage driver's signed URL (5-minute TTL for images, `private, max-age=60`, `noindex`, no referrer). The renew page uses it for the member's photo. An expired, forged or unknown link is a plain 404. (4) The driver is recognised **structurally** (`name === 'local'` plus a `verifySignedUrl` function), not with `instanceof`: Next bundles a route handler separately from the container module, so the same class arrives as two constructors and `instanceof` silently fails — it returned 404 for every valid signed URL until this was found by the renewal journey. **Consequences:** sign-up now has two entry paths to keep working, covered by `e2e/join-modal.spec.ts` (including axe on the open sheet) and by journeys 3–5 for the full pages. Any future parallel slot must ship a `default.tsx` and consider what happens on soft navigation.


## ADR-039 — Max Register: the CRM slice built for the first demo
2026-09-12 · Accepted · **Context:** the client asked for a demo the same day. Phase 4 in the prompts is seventeen items (login, permissions, dashboard, members, profile, add-member wizard, renew, fees, calls, verify, attendance, birthdays, leads, reports, settings, privacy actions, PWA). That is several sessions of work, so the demo-critical spine was built first and the rest deliberately left. **Decision:** (1) **Built now:** PIN login with lockout and sessions; the permission matrix (every capability × role, unit-tested); Home "आज" with the four tiles, calls, birthdays and money; the member list with search and fee filters; the member profile with fee card, attendance dots and history; and **taking fees in three taps** (`recordDeskPayment`) with cash/UPI/card, which reuses the same receipt counter, membership confirmation, activation and outbox as an online payment — one receipt series, whichever way the money arrives. Calls list is read-only. (2) **Not built yet:** add-member wizard, call outcomes, verify queue, attendance marking, leads, reports, settings, privacy exports, undo bar, PWA. Tapping a tab that does not exist yet lands on a plain "coming soon" screen rather than a 404. (3) **Money is owner-only** (`money.view`): reception sees the same Home without the totals, enforced server-side. (4) **Sensitive actions need a PIN entered in the last 5 minutes** (`payment.void`, `settings.manage`, `member.export`); signing in counts as that entry. (5) **The CRM's language comes from a cookie, not the URL** (CLAUDE.md §2.9). next-intl had nothing to infer for `/crm`, so every screen rendered in the website's English default while the `lang` attribute said Hindi; `i18n/request.ts` now falls back to the cookie and then to Hindi. (6) **Server actions** carry CRM mutations (login, take fees): the PIN never reaches client code and the session cookie is set in the same round trip. A successful action redirects, and **Next signals a redirect by throwing** — a `try/catch` around the call swallowed it and left the login screen sitting there, so both call sites now rethrow `NEXT_REDIRECT`. **Consequences:** the demo shows real seeded data (215 members, real fee states, real money totals). Anything under "not built yet" must not be demonstrated as working; the ADR-037 note about demo payments applies here too — a desk payment in demo mode is a real row with a real receipt number, so demo data should be re-seeded before a client demo.

## ADR-040 — Call outcomes, the P9 void, and what a PIN re-entry costs
2026-09-12 · Accepted · **Context:** ADR-039 shipped the CRM spine with the calls list read-only and P9 (voiding a desk payment) unbuilt — the one case left open in the P1–P9 matrix. Both are daily-loop work: a call that cannot be recorded is a call that gets made twice, and money typed in wrongly has to have a way back out. **Decision:** (1) **Call outcomes** (`recordCallOutcome`, BR-7): six answers as six full-width buttons under each call — will renew, call later, no answer, done, wrong number, left the gym. The rules stay in `applyOutcome`: "will renew" checks back in two days, "no answer" retries the next day and gives up after the third attempt, "left the gym" closes the task and marks the member LEFT with the note. The service refuses a task that someone else already closed (`CONFLICT`) rather than recording a second outcome. (2) **P9, voiding a payment:** the payment becomes VOIDED with the reason and who did it, the membership it confirmed goes back to PENDING_PAYMENT, and an audit row records before and after. **The receipt number stays on the voided payment** — a receipt handed to a member cannot be un-issued, and reusing its number would break the gapless series (BR-11.2); the next payment takes the next number rather than filling the gap. The row is locked with `FOR UPDATE`, so two owners tapping void at the same moment void it once. (3) **A PIN re-entry costs an attempt.** Elevation uses the same PIN as the login, so it reuses the same five-attempt lockout: without that, this screen would be an unlimited PIN oracle that walks straight around the login limit. Elevation is not a separate flag either — it is `Session.lastSeenAt`, the same column `actorFor` already reads, so it survives the round trip and lapses on its own after five minutes. (4) **`mayAfterPinEntry(actor, capability, now)`** answers "would entering the PIN be enough?" so a screen knows whether to offer it at all — a receptionist is never asked for a PIN to void a payment, because no PIN would help. It grants nothing: the action still goes through `can`, which refuses until a PIN has actually been entered, and the server checks the role before it will even look at a PIN, so a wrong role never learns whether the PIN was right. (5) **P9 is tested in `packages/db/tests/crm-desk.integration.test.ts`**, not with P1–P8, because it belongs to the desk rather than the gateway; the P1–P9 matrix is now complete. **Consequences:** the calls list is no longer read-only, so a demo that taps an outcome really does change seeded data. Voiding a demo payment leaves a VOIDED row with its receipt number and a membership back at "fees due" — correct, and visible in the demo, so re-seed before showing it.

**Note found while doing this:** `TEST_DATABASE_URL` is **empty** in `.env`, so every database integration suite skipped itself silently — `pnpm test` was green with 35 integration tests never running (ADR-010 chose that skip so a fresh clone stays green). These runs set it explicitly from the direct connection. It should be filled in locally and in CI, or the safety net is decorative.

## ADR-041 — Adding a member at the desk
2026-09-12 · Accepted · **Context:** ADR-039 left the add-member wizard unbuilt, so the only way into the gym was the website: a walk-in standing at the counter had to be sent to a phone to sign themselves up. The desk needed its own door in, without growing a second definition of who may join. **Decision:** (1) **`registerAtDesk` reuses the website's parts** — the same `RegistrationStore` and unit of work (no new repository), the same `RegistrationFieldsSchema`, and the same `assessAge` and `registrationConsents` rules. "At least 16", "a minor's own tick is not enough for face attendance" (BR-12.2) and "what counts as a name" are decided once and cannot drift between the two doors. (2) **The photo is optional at the desk.** A camera that will not open must not stop someone joining; enrolment for face attendance is queued on payment and only for a member who consented, so a missing photo costs nothing today. The service stores one when it is taken and deletes it again if the transaction fails. (3) **The staff member is recorded:** `Member.createdById`, and `Consent.recordedById` with `channel = "crm_desk"` on every consent row — a consent taken across a counter should say who took it. Both columns already existed and are now mapped. (4) **No continuation token.** Nobody is leaving the counter, so the wizard hands straight over to the renew flow, which confirms the membership, assigns the member code and prints the receipt — the desk's normal "3 taps". (5) **The source is `WALK_IN`**, distinct from `WEBSITE` and `QR_NEW`, so reports can tell how people actually arrive. (6) **The server re-validates and answers with a field code**, and the wizard jumps back to the step that needs fixing. That path was proved by accident: an end-to-end test typed a name containing digits, the server refused it, and the screen returned to the name question — which is exactly what should happen when someone mistypes at the desk. **Consequences (ADR-041):** a member added at the desk sits in `PENDING_PAYMENT` until fees are taken — the same state an unpaid website sign-up is in, and picked up by the same nightly call task. The wizard has no camera step yet beyond "not now"; the selfie sheet the website uses can be dropped in later without touching the service, which already accepts a photo.

## ADR-042 — Marking attendance by hand, and what "not coming" means
2026-09-12 · Accepted · **Context:** the attendance tab was a "coming soon" screen, and the kiosk that will record visits automatically is Phase 7. Until then every visit is marked by hand at the desk, so this is not a stopgap — it is how attendance works for the first months of use. Two things were missing underneath: nothing maintained `Member.lastAttendanceAt` at runtime (only the seed set it), and there was no query for members who have stopped coming. **Decision:** (1) **`markAttendance` reuses the kiosk's rule.** BR-9.1's cooldown is the same `cooldownDecision` function the kiosk will call, so "one visit per ⚙ 180 minutes" cannot mean two different things depending on which door recorded it. (2) **Every tap carries a `clientEventId`**, and a repeat of one already stored writes nothing (`DUPLICATE_EVENT`). A double press at a busy counter, or a retried request, is one visit; the column was already unique for exactly this reason. (3) **Undo voids the row, it does not delete it.** Every read already ignores voided events, and a correction that leaves no trace is not a correction — the row records that someone marked this and then took it back. (4) **`Member.lastAttendanceAt` is maintained in the same transaction as the event**, and rewound on undo to the previous visit that still counts, or to "never came". It is what the cooldown is measured from and what "not coming" is counted from, so it has to be true rather than nearly true. **The kiosk must do the same when it arrives** (Phase 7) or both readings drift. (5) **The fee state the desk could see is stored on the check-in** (`feeStateAtCheckIn`), so the `EXPIRED_BUT_VISITING` follow-up (BR-7, priority 1 — the warmest lead the gym gets) can be raised from it later; raising it is not part of this slice. (6) **Staff may mark anyone, whatever their status** (BR-9.4): the kiosk's eligibility rules (BR-9.5) are about whose face may be matched, not about who is standing at the counter. (7) **"नहीं आ रहे" = ACTIVE, paid or due soon, last visit older than ⚙ `absentDaysThreshold` (7 days) or never** — an expired member is already on the fees list, so putting them here too would just be the same list twice. **Consequences:** attendance counts on Home and on the member profile now move during a demo, because marking one in is a real event. `MemberSearch` gained a `basePath`: it was hardcoded to the members list, so searching on the attendance screen navigated away from the mark button — found by the end-to-end journey, not by reading the code.

## ADR-043 — The enquiries pipeline in the CRM
2026-09-12 · Accepted · **Context:** enquiries have been arriving since Phase 2 — the website's lead form writes them, raises the owner's bell alert and queues a WhatsApp alert, and the worker's `lead-followup` job raises a `NEW_LEAD` call task for anything untouched for two gym hours. But the CRM had no screen for them, so all 25 seeded enquiries (and every real one) were invisible to the owner. **Decision:** (1) **`advanceLead` implements BR-10.1** — `NEW → CONTACTED → TRIAL_BOOKED → VISITED → CONVERTED | LOST` — with **skips allowed and backwards refused**. Real enquiries phone at noon and walk in at six, so insisting on each step in turn would only teach staff to lie to the screen; but a status that can move both ways tells the owner nothing about whether an enquiry is still worth a call. `CONVERTED` and `LOST` are final: reopening one is a `CONFLICT`. (2) **Notes are appended with the IST date, never replaced**, the same shape the website's repeat-enquiry note already uses, so one enquiry reads as a history. (3) **No new capability.** Enquiry work is member work, so it reuses `member.edit` (owner and reception, not trainers) rather than growing the permission matrix — every capability there is enumerated and tested, and one more row that means the same thing is a liability. (4) **A separate unit of work from `PrismaLeadUnitOfWork`:** that one creates and merges enquiries arriving from the website; this one only ever moves an existing enquiry forward. (5) The screen lives under **"और"**, with Open (anything not converted or lost) and All tabs, and offers only the statuses this enquiry can still reach. **Not built, and listed as pending rather than assumed:** BR-10.2's automatic conversion when someone registers with the same mobile within 60 days is still unimplemented anywhere in core, db or the worker; and marking an enquiry `CONTACTED` does not yet close an open `NEW_LEAD` call task, so the calls list can still show one the owner has already handled from the enquiries screen. **Consequences:** the demo can now follow a real path end to end — an enquiry typed on the website appears here, gets called, and is marked joined or lost.

## ADR-044 — The demo deployment on Vercel, and what it does not do
2026-09-14 · Accepted (client request: a live URL to check before the client demo) · **Context:** the code went to `github.com/Kunalmishra77/max-fitness` and the web app to a new Vercel project, `max-fitness`, alongside the database in Mumbai. The deployment plan (Phase 8) assumes a container host; Vercel is a demo stage, chosen because the client already has an account. **Decision:** (1) **Root Directory = `apps/web`**, with its `vercel.json` there: pnpm installs the whole workspace, `pnpm build` runs so the MediaPipe copy step still happens, and functions run in `bom1`, next to Supabase. Vercel's Next builder looks for `next` in the root directory, so a root-level config could never work; the setting is a dashboard field with no CLI flag, so the client set it. (2) **`output: 'standalone'` is kept for the container image but switched off when `VERCEL=1`**, because Vercel packages each route itself. (3) **Demo mode in production is explicit:** `DEMO_MODE=true` and `ALLOW_DEMO_IN_PRODUCTION=true`, the second being the guard `parseEnv` requires precisely so this cannot happen by accident. WhatsApp uses the simulator. Secrets were copied from `.env` into Vercel without being printed. (4) **`prisma generate` runs on install** (`packages/db` `postinstall`): the generated client is gitignored, so a clean checkout — Vercel, CI, a new laptop — could not build at all. **Two production-only bugs, both caused by the CRM's locale cookie (ADR-039 point 5), found only because this was deployed:** first, `next build` had been failing since the CRM slice, because the cookie was read during static generation — every check since had used `next dev`; second, the first fix wrapped that read in try/catch, which let the build pass but made `/` and `/hi` switch from static to dynamic at request time, and Next answered with a 500. The cookie is now read only when the URL carries no locale — the CRM — so the website's pages never touch it. **What this deployment does not do:** photos and receipt PDFs are written to `/tmp`, which does not survive between function instances, so they may not open later — a real bucket (S3/R2) is needed before members use it; **the worker does not run on Vercel at all**, so outbox dispatch, reminders and nightly call tasks are not happening (the health route correctly reports the worker as down); and payments are simulated. **Consequences:** fine for showing the client the site and Max Register; not a production launch. Every push to `main` now also deploys, since the repository is connected.

## ADR-045 — Four rules the CRM was skipping
2026-09-14 · Accepted · **Context:** ADR-042, ADR-043 and the progress log listed four gaps as pending rather than pretending them done. Each left a screen the owner already uses saying something untrue: the calls list missed the most valuable call of the day, still showed enquiries someone had already rung, enquiries that had joined stayed "open" forever, and a passing database hiccup signed staff out. **Decision:** (1) **`EXPIRED_BUT_VISITING` is raised at the desk** (BR-7 priority 1, BR-9.3). `markAttendance` calls the existing `shouldCreateExpiredButVisiting` rule — ACTIVE member, fee state EXPIRED — for a visit that was actually recorded, due today. The partial unique index keeps it to one open task per member, and `skipDuplicates` makes a second walk-in a no-op rather than an error; that was proved against Postgres, not assumed. The desk hears about it at once, in the undo bar, rather than tomorrow morning. (2) **Moving an enquiry closes its open `NEW_LEAD` calls** (BR-7 "tasks auto-close when the underlying condition clears"). Any move means someone has dealt with it, so "not contacted in two gym hours" has stopped being true; `shouldAutoClose` returned `false` for `NEW_LEAD`, so nothing had ever closed one. A refused move closes nothing. (3) **BR-10.2 runs inside the registration transaction, for both doors** — the website's `registerMember` and the desk's `registerAtDesk`. Every enquiry from the same number made in the last 60 days that is not already converted becomes `CONVERTED` with `convertedMemberId`, and its open `NEW_LEAD` calls close. **A `LOST` enquiry converts too:** joining is the fact that matters, and the pipeline's "no reopening" rule is about staff edits, not about the system recording that someone joined. Conversion happens at registration, as BR-10.2 words it, so someone who registers and never pays still counts as converted; if the owner wants "converted" to mean "paid", that is a rule change to raise with the client. (4) **The session lookup is retried once on failure** (`withOneRetry`), and never on an answer — `null`, "there is no session", is returned at once, because retrying it would only make a signed-out person wait twice as long. **Consequences:** the calls list and the enquiries screen now agree with each other and with the member list. The retry reduces the sign-outs; it does not explain the one unexplained login flake of 2026-09-12, which remains open.

## ADR-046 — The owner's reports ("हिसाब")
2026-09-14 · Accepted · **Context:** crm-module-spec §6 lists ten report cards and crm-ux-blueprint §13 asks for one number and one small figure each, no tables for the owner. The spec names the metrics but not their edges — which month, what counts as a renewal rather than a comeback, when a renewal rate is final — and a report is only useful if the owner can trust the number the day it appears. **Decision:** (1) **Split: the database fetches, `packages/core/src/reports` decides.** `PrismaReportsReader` returns rows; pure, unit-tested functions turn them into the cards. (2) **Edges, chosen and tested:** "this month" is the IST calendar month; money is PAID payments by the IST day they were paid, **demo-gateway payments shown apart and never added to income** (ADR-037); a **new member** is one whose first confirmed membership was confirmed this month; a **renewal** is confirmed this month and starts within the grace period of the member's previous membership (BR-3.4) — someone back after the grace period is neither, they returned; the **renewal rate** counts only memberships that ended this month *and whose grace has already run out*, so it does not drop every morning and recover by the weekend; **busy hours** are per-day averages over 28 days (every weekday and weekend day four times), weekdays and weekends apart, because raw counts would make every weekday look busier; **plan mix** covers 90 days; **percentages use largest remainder** so a mix always adds to 100. (3) **Forms and colour by the data-viz method, not by taste:** busy hours are two small multiples on one shared scale rather than two series on one plot; plan mix and money by method are single-series bars in one hue with the value at the tip; men/women is the only two-series figure and so the only one with a legend; renewal rate and face attendance are meters. Colours come from the documented reference palette — categorical slots 1–2 and its blue ramp — because the CRM's own tokens have no data ramp; the pair was validated against the CRM's white surface (all six checks pass: CVD ΔE 24.7, normal-vision ΔE 33.6) and the single mark hue clears 3:1 (4.42:1). Text stays in the CRM's text tokens; every value is readable without hovering, and the column chart has a table twin. The CRM is light-only, so there is no dark set. (4) **Owner-only** (`money.view`): reception gets a plain notice, and the link is hidden from them. (5) **"Reminder impact" is named, not measured:** it needs the WhatsApp engine (Phase 6), and a card showing zero would say reminders do not work. **Found along the way:** two integration-test gyms (13 members each) had been left in the shared database by the 2026-09-12 run whose teardown failed on the attendance foreign key; the reports' first data check picked one of them up instead of the demo gym. They were listed, confirmed to hold only test rows, and deleted by id; the demo gym was untouched. **Consequences:** the numbers on the cards match what the owner would count by hand. A test run that fails in teardown can still leave a throwaway gym behind — they are prefixed `gym_it_`, never visible to the app, and should be swept before a real launch.

## ADR-047 — Settings, first slice: prices, joining rules, website offer, trust, hours
2026-09-14 · Accepted · **Context:** crm-ux-blueprint §14 lists a dozen settings. Several open client questions — the 3/6/12-month prices (placeholders in BR-2.2), the admission fee, the minimum age — are values the owner should be able to set alone rather than wait on a release. **Decision:** (1) **This slice covers plan prices, the admission fee, the minimum age, the website's offer, trust numbers and opening hours.** Staff management, kiosk pairing, reminder times, language and voice, and the kill switch follow in later slices. (2) **Everything here needs `settings.manage`: the owner, with a PIN entered in the last five minutes.** Opening the screen without a fresh PIN shows a PIN gate. If the PIN lapses while the owner is typing, the section asks for it in place and then saves what was typed — the form is not reset. The server checks the role before it looks at a PIN, so a receptionist never spends an attempt. (3) **Prices are whole rupees between ₹100 and ₹1,00,000**, set on the `Plan` rows. Only prices that actually changed are written, and **BR-2.8 holds by construction**: a membership copies its price at sale, and nothing here touches a membership. (4) **`updateGymSettings` changes only an allowlist of fields** — `pricing.admissionFeePaise`, `privacy.minAge`, `promo.enabled/textEn/textHi`, the five trust numbers, and the whole week of hours. Anything else, such as a feature flag, is refused rather than silently ignored. The merged document is validated by the same `GymSettingsSchema` every reader uses. (5) **The gym row is locked for the save** (`FOR UPDATE`): settings are one JSON document, so two phones saving at once would otherwise each read the old document and the second would undo the first. (6) **An audit row is written only when something changed**, with before and after for just the changed groups, so the audit log lists real changes, not button presses. (7) **A change that shows on the public site refreshes the landing cache at once.** `revalidateLandingContent` (ADR-022) now calls **`updateTag`**, which expires the cached entries immediately and may only be called from a Server Action — the only caller. It previously called `revalidateTag(tag, 'max')`, which in Next 16 is stale-while-revalidate: the first visitor after a save still got the old page, and that visitor is usually the owner checking their change. The end-to-end test caught it by saving an offer and looking for it on `/hi`. (8) **Phone layout uses explicit widths where it matters.** This project's design tokens put the `sm` breakpoint at **360px**, not Tailwind's 640px, so `sm:grid-cols-2` applied on a phone and squeezed each price field to 30px. The price tiles now switch to two columns at `min-[480px]`; the rendered grid was measured in the browser rather than assumed. **Consequences:** the placeholder prices and joining rules stop blocking delivery — the owner can set them on the day. A price change applies to the next fee taken, never to anyone already paid up.

## ADR-048 — Staff logins, and reception taking fees as its own setting
2026-09-14 · Accepted · **Context:** the launch checklist (security-plan §6) says owner and staff PINs are set by them and the demo users do not survive into production, and crm-module-spec §3 lists "Settings, staff, kiosk pairing — owner + PIN". Until now the only logins were the two seeded demo users, and the owner had no way to add reception, change a forgotten PIN or remove someone who had left. **Decision:** (1) **A staff screen under "और", behind the same `settings.manage` gate as settings** (owner with a fresh PIN). It adds reception or trainers, resets a PIN, and switches someone off or back on. (2) **Nobody becomes an owner, and no owner is locked out, from this screen.** Only RECEPTION and TRAINER can be added; an owner's PIN cannot be reset here and an owner — including yourself — cannot be switched off. An owner changes their own PIN with the current one, in a flow of its own (next slice). (3) **A PIN never reaches the audit log**, neither the digits nor the hash; the audit records who was added, reset or switched off, and by whom. (4) **A PIN reset or a switch-off revokes every live session of that person at once**, and a reset also clears any lockout from wrong guesses. A PIN that changed while an old session lived on would make the change decorative; the end-to-end test proves it by signing a new trainer in on a second browser, switching them off from the owner's, and watching the trainer's next request land on the login screen. (5) **Names and PINs use one shared Zod schema** (`StaffCreateSchema`), with the member name rule reused, so "what counts as a name" is decided once. (6) **Hashing is a separate `PinSetter` port**; `PinHasher` stays verify-only, so the login and PIN re-entry code never gains the ability to write a hash. **A latent bug fixed along the way:** whether reception may take fees is a setting in the permission matrix, but no such setting existed, and `currentActor` passed `pricing.allowDeskDiscounts` in its place — so switching discounts off would also have stopped reception taking fees. It is now `pricing.receptionMayTakePayments` (default on), allowlisted in `updateGymSettings`, with a toggle on the staff screen. **Consequences:** the demo PINs can be replaced on handover day, and someone who leaves loses access the moment the owner switches them off. The dev server crashed once during this slice with a Turbopack internal panic (a Next.js bug, exit `0xC0000409`), unrelated to the change; it was restarted.

## ADR-049 — Changing your own PIN
2026-09-14 · Accepted · **Context:** ADR-048 let the owner manage everyone else's login, but deliberately not an owner's own PIN — and the launch checklist (security-plan §6) says every PIN is set by the person who uses it, with the demo owner PIN `2468` gone before real use. **Decision:** (1) **"मेरा PIN बदलें" is open to every role**, from "और" and from the staff screen. It is the one account setting each person owns. (2) **The current PIN is the proof**, so no fresh-PIN gate sits in front of a form that already asks for the PIN. The new PIN is typed twice; it must be four to six digits and differ from the current one. (3) **Checking the current PIN spends the login screen's five attempts and its 15-minute lockout.** A locked account is refused without spending an attempt, and a switched-off account is refused exactly like a wrong PIN, so the screen reveals nothing. (4) **A wrong guess is recorded inside the transaction but reported after it.** Throwing inside the transaction would roll the failure count back along with everything else, and the lockout would never build up — a unit test with an in-memory store cannot see this, because nothing rolls back there. The database integration test checks the count survives a wrong guess. (5) **On success the PIN is replaced, any lockout cleared, and every *other* session revoked**; the session the change was made from, identified by its token's hash, stays signed in. (6) **No PIN or hash reaches the audit log**, which records only that a change happened and who made it. **Consequences:** the demo owner PIN can be changed by the owner on handover day, and a PIN someone else has seen can be changed without leaving old sessions alive.
